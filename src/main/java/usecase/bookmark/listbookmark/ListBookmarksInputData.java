package usecase.bookmark.listbookmark;

/**
 * Input data for the "list bookmarks" use case.
 * Currently, this use case does not require any parameters, but the class
 * is kept to preserve the Clean Architecture pattern and to allow for
 * future extensions (e.g., filtering or sorting options).
 */
public final class ListBookmarksInputData {

    /**
     * Constructs input data for listing bookmarks.
     * No parameters are required at the moment.
     */
    public ListBookmarksInputData() {
        // Intentionally empty.
    }
}
